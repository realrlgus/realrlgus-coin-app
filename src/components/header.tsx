import styled from "styled-components";
import { IHeader } from "../interface";

const HeaderContainer = styled.div``;

export const Header = ({ toggleTheme }: IHeader) => {
  return <HeaderContainer></HeaderContainer>;
};
